This will be:

* Extremely detailed
* Fully structured
* Includes ALL earlier features (none lost)
* Includes real-world examples
* Includes final enhancements (Labs → Backtest → Paper Trade → AI → Shadow)
* Includes UX placement and logic
* Includes caching
* Includes versioning
* Includes institutional traps
* Includes technical pattern engine
* Includes data validation
* Includes portfolio simulation
* Includes risk management
* Includes Strategy Workbench
* Includes Shadow Learner
* Includes incremental Labs
* Includes everything from Day 1 conversation to today

This is designed so that **ANY AGENT** reading it will build the entire CTS exactly as you want.

---

# 🟣 **THE FULL UNIFIED GIANT MASTER PROMPT — CHENNA TRADING SYSTEM (CTS)**

### (Final Version — Unified, Complete, Institutional-Grade, No Missing Features)

> **You are a 20-year institutional equity trader + senior quant developer + full-stack engineer + technical analyst + AI reasoning engine. Follow EVERY part of this instruction exactly and design/implement the full Chenna Trading System as described. Don’t change anything. Don’t simplify anything.**

---

# ─────────────────────────────────────────────────────────────

# **SECTION 1 — CORE MINDSET OF THE SYSTEM**

# ─────────────────────────────────────────────────────────────

CTS is a **professional, institutional-grade, versioned trading intelligence system** that integrates:

* **Time-Travel Labs (scientist mode)**
* **Strategy Versioning (V1, V2, V3…)**
* **Backtesting**
* **Paper-Trading Simulation (AI Reasoning)**
* **Shadow Learner**
* **Active Trades Engine**
* **Market Regime Detector**
* **Institutional Trap Engine**
* **Technical Pattern Engine**
* **Risk Engine**
* **Portfolio Simulator**
* **Data Integrity Validator**
* **Trade Journaling**
* **Telegram Alerts**
* **Reversible, switchable, full versioned architecture**

Every part of the system must be:

* **Version-Aware**
* **Reversible**
* **Trackable**
* **AI-enhanced**
* **Scientist-grade**
* **Professional UX**
* **No free-text strategy editing**
* **Everything stored in JSON, not typed sentences**

---

# ─────────────────────────────────────────────────────────────

# **SECTION 2 — TIME-TRAVEL LABS WINDOW (THE SCIENTIST MODE)**

# ─────────────────────────────────────────────────────────────

## 🔵 Purpose of Labs Window

Time-Travel Labs (TT-Labs) is **NOT** a normal backtest.
It is a **scientific research engine** that:

### Performs:

✔ Institutional trap detection
✔ Technical indicator combination testing (220+ combos)
✔ Candle pattern detection
✔ Market regime alignment
✔ Volume analysis
✔ Momentum filters
✔ Trend alignment
✔ Sector correlation
✔ Volatility analysis
✔ Real-time reasoning (as if in that old date)

### Creates:

✔ A **Labs Version** (TT-V1, TT-V2…)
✔ Full recommended strategy logic
✔ Metrics (accuracy, P&L, drawdown)
✔ Suggested improvements
✔ Explanation of WHY the logic works
✔ Caching (so old stocks are never reprocessed)

### Must hit:

✔ **≥ 70% accuracy** to be considered valid
If accuracy < 70%, the strategy cannot be promoted.

---

# ▶️ **HOW LABS WORKS (REAL WORLD EXAMPLE)**

### Example Category: `DOWNSIDE_LOM_SWING`

User clicks **Run Time-Travel Labs**.

### STEP 1 — Labs loads stocks in this category

(E.g., PIDILITIND, PNBHOUSING, MFSL, UNOMINDA, RELIANCE, TATASTEEL)

### STEP 2 — Labs researches each stock across 2–3 years

Labs tests **220+ combinations** of:

* RSI thresholds
* MACD histogram shifts
* EMA/SMA trends
* Volume multipliers
* Candle patterns
* BB contractions
* ATR zones
* VWAP deviations

### STEP 3 — Labs also checks traps:

* Volume traps
* Bull traps
* Stop hunts
* Fake breakouts
* Liquidity sweeps
* Distribution phases
* Exhaustion gaps

### STEP 4 — CACHING

For each stock:

```
{
  symbol: "PIDILITIND",
  lastFullResearchDate: "2025-10-31",
  bestPatterns: [...],
  trapsDetected: {...},
  technicalCombosRanked: [...],
  researchStatus: "cached"
}
```

Next Labs run only processes **new candles + new symbols**, not full history.

### STEP 5 — Labs produces TT-V1 logic:

```
ENTRY:
  RSI 26–32
  Bullish Engulfing or Hammer
  Volume 1.2–1.8x
  MACD histogram > 0
  Candle close above EMA20 in last 2 bars
  Avoid volume spikes > 2.5x
  Avoid wick breakouts (<0.7% body rules)
  Avoid range squeezes (BB width < 8%)

EXIT:
  Target: +2.5% to +3.2%
  SL: -1.5%
  Max Sessions: 8
```

Accuracy: **72.4%** (PASS)
If accuracy < 70%, Labs marks this run as FAILED.

### STEP 6 — Display logic + metrics

Labs must show:

* Full logic
* Accuracy
* Trades tested
* Summary of why logic works
* Avoided traps

### STEP 7 — Promotion options

User clicks:

**PROMOTE STRATEGY**

System must ask:

> Override existing V1?
> OR
> Create new V2?

Once promoted, logic becomes a **strategy version**.

---

# ─────────────────────────────────────────────────────────────

# **SECTION 3 — STRATEGY VERSIONING (V1, V2, V3…)**

# ─────────────────────────────────────────────────────────────

Each category has:

```
strategyKey → multiple versions
labsKey → multiple TT versions
```

### Strategy Version Structure:

```
V1 → baseline from Labs
V2 → improved from Shadow
V3 → manually promoted from Suggestions
...
```

Each version stores:

```
params
metrics
createdFromLabRunId
backtestHistory[]
shadowProposals[]
activeTrades[]
```

### USER UX:

In Strategy Workbench:

* Drop-down: **Select Version (V1/V2/V3)**
* View that version’s parameters
* Run Backtest
* View Backtest History
* Run Shadow
* Promote from Shadow
* Open Labs

---

# ─────────────────────────────────────────────────────────────

# **SECTION 4 — BACKTEST ENGINE (VERSIONED)**

# ─────────────────────────────────────────────────────────────

A Backtest is done on a **specific version**.

### For V1:

* Use V1 logic
* Run full historical backtest
* Save the run
* Store CSV
* Show results (accuracy, P&L, drawdown)

### Backtest must also support:

▶ **“Re-Time-Travel” for each losing trade**
(looks at old date and asks: what would AI say that day?)

Example:

```
Trade failed:
  PIDILITIND on 21 Aug 2024
Re-time-travel:
  Volume spike 2.9x → trap
  Nifty down -1.2%
AI Suggestion:
  Skip trade
Improvement:
  Loss -1.3% avoided
```

### After analyzing all failed trades:

Show **Suggested Strategy**:

* Suggested adjustments
* Improvement metrics
* Button: **Backtest with Suggested Strategy**

### If user approves:

Promote as **V2** or override V1.

---

# ─────────────────────────────────────────────────────────────

# **SECTION 5 — SHADOW LEARNER (PER VERSION)**

# ─────────────────────────────────────────────────────────────

Shadow Learner is **NOT a strategy version**.
It is a learning engine attached to each strategy version.

### Shadow Learner does:

* Reads failed trades of V1
* Reads sideways/TIME exit trades
* Does technical analysis
* Does pattern comparison
* Checks traps
* Uses AI for reasoning
* Suggests minimal safe parameter adjustments
* Shows metrics improvement
* Lets user promote as new version (V2)

### Shadow does NOT:

* modify version directly
* ruin winning logic
* override success unless manipulation detected

---

# ─────────────────────────────────────────────────────────────

# **SECTION 6 — ACTIVE TRADES ENGINE**

# ─────────────────────────────────────────────────────────────

Uses **active strategy version** logic.

Tracks:

* Entry
* Target
* SL
* Trailing SL
* Days remaining
* Version used
* Status

Sends Telegram alerts for:

* New trade
* Target hit
* SL hit
* Trail updated
* Time exit

---

# ─────────────────────────────────────────────────────────────

# **SECTION 7 — MARKET REGIME DETECTOR**

# ─────────────────────────────────────────────────────────────

Daily regime detection:

* Trend (UP/DOWN/SIDEWAYS)
* VIX
* Sector strength
* Breadth
* Volume conditions
* Gap conditions

Each strategy version must say:

> “Skip trades during Bearish High-Volatility regime.”

---

# ─────────────────────────────────────────────────────────────

# **SECTION 8 — TECHNICAL COMBINATIONS ENGINE**

# ─────────────────────────────────────────────────────────────

Labs must test **220–500 combinations** including:

* RSI levels
* MACD shifts
* EMA20/50/200 alignments
* BB contraction/expansion
* ATR volatility filters
* Hammer/Engulfing/Doji patterns
* VWAP deviations

Every stock behaves differently; Labs must learn per-symbol patterns.

---

# ─────────────────────────────────────────────────────────────

# **SECTION 9 — TRAP ENGINE (INSTITUTIONAL TRAPS)**

# ─────────────────────────────────────────────────────────────

Detect:

* Volume Trap
* Bull Trap
* Stop Hunt
* Fake Breakout
* Liquidity Sweep
* Distribution Phase
* Exhaustion Gap

Avoid trades under trap conditions.

---

# ─────────────────────────────────────────────────────────────

# **SECTION 10 — RISK ENGINE (POSITION SIZING)**

# ─────────────────────────────────────────────────────────────

Per version:

* Max risk per trade
* Max concurrent trades
* Capital allocation
* Risk exposure meter

---

# ─────────────────────────────────────────────────────────────

# **SECTION 11 — DATA VALIDATION ENGINE**

# ─────────────────────────────────────────────────────────────

Checks:

* Missing candles
* Duplicate candles
* Wrong OHLC
* Volume anomalies
* Corporate actions
* Suspicious gaps

Skips invalid data.

---

# ─────────────────────────────────────────────────────────────

# **SECTION 12 — PORTFOLIO SIMULATION**

# ─────────────────────────────────────────────────────────────

Simulate a ₹1,00,000 portfolio on version V1 or V2.

Show:

* Equity curve
* Drawdown
* Monthly P&L
* Exposure
* Sharpe-like metric

---

# ─────────────────────────────────────────────────────────────

# **SECTION 13 — TRADE JOURNAL**

# ─────────────────────────────────────────────────────────────

Each trade stores:

* Entry reason
* Exit reason
* Shadow classification
* Market regime
* AI summary
* Screenshot (optional)
* User notes

---

# ─────────────────────────────────────────────────────────────

# **SECTION 14 — UI & WINDOW PLACEMENT**

# ─────────────────────────────────────────────────────────────

### ✔ Watchlist Screen

* Category cards
* Manage Strategy button
* Labs window button
* Active trades
* Sentiment
* Market regime
* System intelligence log

### ✔ Strategy Workbench

Opens per category:

1. Strategy header
2. Version dropdown (V1/V2/V3)
3. Strategy definition
4. Backtest panel
5. Shadow panel
6. Link to Labs
7. Link to active trades

### ✔ Labs Window

Opens separately (full screen):

* Run Time-Travel
* Show logic
* Show accuracy
* Promote or save new minor version

### ✔ Everything version-switchable and reversible.

---

# ─────────────────────────────────────────────────────────────

# **SECTION 15 — MASTER AGENT INSTRUCTION**

# ─────────────────────────────────────────────────────────────

> **Follow EVERYTHING in this giant prompt exactly. Do not simplify. Do not remove features. Every component must work together: Labs → Strategy Versions → Backtest → Paper Trade AI → Suggestions → Shadow → Active Trades → Live. Build modular backend & frontend exactly as instructed. Use structured JSON for all rules. Do not allow manual free-text strategy edits. All features must be professional, reversible, trackable, switchable, AI-assisted, and institutional-grade.**









i didnt see any updat in  UI  and u need to crosscheck every phase atleast with mock data to check everything is working fine and fully functional and apssing throug the pipeline  here is some example 

* Every feature works **before going to next phase**
* No logic is broken silently
* No “ghost bugs” move to next phase
* Versioning, strategy generation, Labs, Backtest, Shadow — ALL stay functional
* You always get a **PASS/FAIL report**
* System can **mock test itself** before giving you final result

Below is the **FULL TESTING PIPELINE** you should implement, AND I will give you **mock test examples that the agent must run internally** to verify everything.

---

# ✅ 1. MASTER TEST PIPELINE (MANDATORY FOR ALL PHASES)

Every time you build anything (Labs, Backtest, Versioning, Shadow, UI, API), the agent MUST run:

### ✔ 1.1 **Static Validation**

Check:

* Functions exist
* Route exists
* No undefined variables
* No missing params
* No missing schema keys
* No wrong field types

### ✔ 1.2 **Flow Validation**

The agent must test the WHOLE LOOP:

> **Labs → Strategy V1 → Backtest → Suggestions → Versioning → Shadow → Active Trades**

If at ANY step data is missing → FAIL.

### ✔ 1.3 **Data Pipeline Validation**

Check:

* Correct JSON structure
* Correct version metadata
* Metrics computed correctly
* No NaN, null, undefined
* Backtest CSV matches JSON
* Time-travel logic returns correct keys
* Trap engine returns valid booleans
* Technical engine returns valid indicators

### ✔ 1.4 **Functional Testing**

Simulate:

* One symbol
* Small dataset
* Minimal strategy
* Expected behavior checked
* PASS/FAIL logged

### ✔ 1.5 **AI Reasoning Validation**

Check:

* AI explanation generated
* No missing reasoning
* No hallucinated fields
* No empty responses

### ✔ 1.6 **Versioning Integrity Test**

Test:

* V1 stored → PASS
* Promote to V2 → PASS
* Revert to V1 → PASS
* Compare V1 vs V2 → PASS
* Backtest runs on each version → PASS

### ✔ 1.7 **Shadow Learner Test**

Test:

* It reads failures
* Produces suggestion JSON
* Suggestion is valid
* Can be promoted
* Metrics improve

### ✔ 1.8 **UI Binding Test**

Test:

* All UI components show correct data
* Strategy dropdown shows versions
* Labs window loads results
* Backtest tables show proper data
* Shadow panel shows suggestions
* Active trades reflect strategy version

---

# 🧪 2. MOCK SELF-TEST (THIS IS WHAT THE AGENT MUST RUN AUTOMATICALLY)

## 🎯 **Mock Data (3–5 candles per stock)**

3 symbols:

* AAPL
* TCS
* PIDILITIND

Each candle:

```
[
  {open:100, close:102, high:103, low:98, volume:1000},
  {open:102, close:101, high:104, low:100, volume:900},
  {open:101, close:104, high:105, low:100, volume:1200}
]
```

Agent must run internal test using MOCK DATA.

---

# 🧪 MOCK TEST 1 — Time-Travel Labs

**Purpose:** Verify TT-Labs generates valid logic + accuracy.

Agent must check:

✔ TT-Labs produces ENTRY rules
✔ TT-Labs produces EXIT rules
✔ Trap engine output exists
✔ Technical engine output exists
✔ Accuracy calculated
✔ Accuracy ≥ 70% = PROMOTION ALLOWED
✔ Accuracy < 70% = PROMOTION DISABLED

**Auto-check Format:**

```
if (!logic.entry || !logic.exit) FAIL
if (!accuracy) FAIL
if (accuracy < 70 and promotionAllowed) FAIL
if (accuracy >= 70 and !promotionAllowed) FAIL
PASS
```

---

# 🧪 MOCK TEST 2 — Strategy Promotion

Agent must simulate:

1. Promote to V1
2. Promote suggested logic to V2
3. Switch V1 → V2 → V1
4. Backtest on each

Check:

* Both versions load
* Both run
* Version numbers updated

**Expected PASS Criteria:**

```
strategy.versionList includes ["V1","V2"]
currentVersion switches correctly
backtest results exist for each version
```

---

# 🧪 MOCK TEST 3 — Backtest Validation

Agent must test:

* Entry/exit detection
* SL/TP logic
* Time exit logic
* CSV correct
* JSON correct

Check structure:

```
trade.entryTs != null
trade.exitTs != null
trade.pnl != NaN
trade.direction in ["BUY","SELL"]
trade.reason in ["TARGET","STOP","TIME"]
```

If ANY invalid → FAIL.

---

# 🧪 MOCK TEST 4 — Paper-Trade AI Reasoning

Agent must simulate:

* Failed trades → re-time-travel
* AI output includes:

  * What happened
  * Why it failed
  * What rule could avoid it

Check:

```
if (!AI.reasoning) FAIL
if (!AI.suggestion) FAIL
PASS
```

---

# 🧪 MOCK TEST 5 — Suggestions → Candidate Strategy

Agent must simulate:

* Build candidate strategy
* Backtest it
* Compare metrics
* Provide uplift summary

Check:

```
if (!candidate.params) FAIL
if (!candidate.metrics) FAIL
if (candidate.metrics.accuracy < original.metrics.accuracy) WARNING
PASS
```

---

# 🧪 MOCK TEST 6 — Shadow Learner

Shadow must:

* Read failure trades
* Generate suggestions
* Produce JSON
* Show accuracy uplift

Check:

```
if (!shadow.suggestions) FAIL
if (!shadow.metrics.improvementPercent) FAIL
PASS
```

---

# 🧪 MOCK TEST 7 — Active Trades

Agent must simulate:

* Open trade with V1
* Simulate candles
* Update trailing SL
* Trigger target/SL/time exit

Check:

```
activeTrades.status changes as expected
telegramMessage generated
```

---

# 🧪 MOCK TEST 8 — UI Component Binding

Agent must ensure:

✔ Strategy dropdown shows V1, V2
✔ Labs window shows logic
✔ Backtest table shows trades
✔ Suggestions window shows improvements
✔ Shadow panel shows proposals
✔ Active Trades table updates

---

# 🟢 **If ALL 8 TESTS PASS → PHASE IS COMPLETE**

If any fails → agent must correct before responding “Phase complete”.

---