name: CI Quick Checks

on: [push, pull_request]

jobs:
  frontend-verify:
    runs-on: windows-latest
    steps:
      - run: echo "frontend-verify stub"
  backend-verify:
    runs-on: windows-latest
    steps:
      - run: echo "backend-verify stub"
  repo-hygiene:
    runs-on: windows-latest
    steps:
      - run: echo "repo-hygiene stub"
