apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ../base
images:
  # override via 'kustomize edit set image' or use --load_restrictor=LoadRestrictionsNone
  - name: cts-run-worker
    newName: ${IMAGE_NAME}
    newTag: ${IMAGE_TAG}
